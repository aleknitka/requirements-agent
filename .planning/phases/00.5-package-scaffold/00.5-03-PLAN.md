---
phase: 00.5-package-scaffold
plan: 03
type: execute
wave: 3
depends_on: ["00.5-01", "00.5-02"]
files_modified:
  - src/requirements_agent_tools/CONSTANTS.py
  - src/requirements_agent_tools/models.py
  - src/requirements_agent_tools/init_project.py
  - src/requirements_agent_tools/req_ops.py
  - src/requirements_agent_tools/meeting.py
  - src/requirements_agent_tools/report.py
  - src/requirements_agent_tools/review.py
  - src/requirements_agent_tools/project_md_cli.py
  - src/requirements_agent_tools/refine.py
  - src/requirements_agent_tools/db/cli.py
  - src/requirements_agent_tools/db/updates.py
  - .pre-commit-config.yaml
autonomous: true
requirements: [PKG-04, PKG-06]

must_haves:
  truths:
    - "uv run interrogate src/ --fail-under 90 exits 0 (actual coverage >= 90%)"
    - ".pre-commit-config.yaml exists with 8 hooks: ruff-format, ruff-check, detect-private-key, bandit, ty, interrogate, lazydocs, pytest"
    - "uv run pre-commit run --all-files completes without any hook failure"
    - "All docstrings use Google style (Args/Returns/Raises sections)"
    - "bandit B608 false positives in db/minutes.py:97 and db/requirements.py:201 and db/requirements.py:284 are suppressed with # nosec B608 comments"
  artifacts:
    - path: ".pre-commit-config.yaml"
      provides: "Pre-commit pipeline definition with 8 hooks"
      contains: "ruff-format"
    - path: "src/requirements_agent_tools/db/cli.py"
      provides: "Documented Click CLI commands"
    - path: "src/requirements_agent_tools/models.py"
      provides: "Documented Pydantic models and enums"
  key_links:
    - from: ".pre-commit-config.yaml"
      to: "pyproject.toml [tool.interrogate]"
      via: "interrogate hook reads fail-under = 90 from pyproject.toml automatically"
      pattern: "fail-under"
    - from: ".pre-commit-config.yaml"
      to: "uv run --with lazydocs lazydocs"
      via: "local hook entry"
      pattern: "lazydocs"
---

<objective>
Write Google-style docstrings across 10 modules to bring coverage from 50.6% to >= 90%,
then create .pre-commit-config.yaml with 8 hooks enforcing that coverage on every commit.

Purpose: The interrogate pre-commit hook requires >= 90% docstring coverage (already set in
pyproject.toml fail-under = 90). Coverage is currently 50.6% (84/166 items). The hook must
not be enabled until coverage passes or it will block every commit immediately.

Output:
  - Google-style docstrings added to ~80 undocumented public items across 10 source files
  - .pre-commit-config.yaml with 8 hooks (ruff-format, ruff-check, detect-private-key,
    bandit, ty, interrogate, lazydocs, pytest)
  - pre-commit installed and verified passing on all files
</objective>

<execution_context>
@/home/aleksander/.claude/get-shit-done/workflows/execute-plan.md
@/home/aleksander/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00.5-package-scaffold/00.5-RESEARCH.md
@.planning/phases/00.5-package-scaffold/00.5-VERIFICATION.md

<interfaces>
<!-- Interrogate coverage table (verified 2026-04-29) — miss counts by file -->
<!--
  db/cli.py         : 23 total,  17 miss  (26% covered)
  models.py         : 20 total,  16 miss  (20% covered)
  meeting.py        : 10 total,   9 miss  (10% covered)
  req_ops.py        : 10 total,   9 miss  (10% covered)
  review.py         : 11 total,   8 miss  (27% covered)
  report.py         :  8 total,   6 miss  (25% covered)
  project_md_cli.py :  7 total,   6 miss  (14% covered)
  init_project.py   :  6 total,   5 miss  (17% covered)
  CONSTANTS.py      :  5 total,   3 miss  (40% covered) — db_path, md_path, slugify
  db/updates.py     :  5 total,   1 miss  (80% covered) — _row_to_record

  Already at 100%: _cli_io.py, project_md.py, project_session.py, db/__main__.py,
                   db/_logging.py, db/_serialization.py, db/connection.py,
                   db/embeddings.py, db/minutes.py, db/projects.py,
                   db/requirements.py, db/schema.py

  Total: 84/166 documented. Need >= 150/166 (90.4%) → must add >= 66 docstrings.
-->

<!-- Bandit B608 false positives (verified 2026-04-29) -->
<!-- Add  # nosec B608 — <column names are hardcoded constants, not user input>  at: -->
<!--   src/requirements_agent_tools/db/minutes.py line 97 -->
<!--   src/requirements_agent_tools/db/requirements.py line 201 -->
<!--   src/requirements_agent_tools/db/requirements.py line 284 -->

<!-- pre-commit config (verified revs 2026-04-29) -->
<!--
  astral-sh/ruff-pre-commit  rev: v0.15.12
  pre-commit/pre-commit-hooks rev: v6.0.0
  PyCQA/bandit                rev: 1.9.4
  econchick/interrogate        rev: 1.7.0
  ty: local hook (no astral-sh/ty-pre-commit repo exists)
  lazydocs: local hook (uv run --with lazydocs)
  pytest: local hook (uv run pytest)
-->
</interfaces>
</context>

<tasks>

<!-- ═══════════════════════════════════════════════════════════════════
     WAVE A: Write docstrings (Tasks 1–3) — must all pass before Wave B
     ═══════════════════════════════════════════════════════════════════ -->

<task type="auto">
  <name>Task 1: Add Google-style docstrings to db/cli.py and db/updates.py</name>
  <files>
    src/requirements_agent_tools/db/cli.py
    src/requirements_agent_tools/db/updates.py
    src/requirements_agent_tools/db/minutes.py
    src/requirements_agent_tools/db/requirements.py
  </files>
  <read_first>
    src/requirements_agent_tools/db/cli.py
    src/requirements_agent_tools/db/updates.py
    src/requirements_agent_tools/db/minutes.py
    src/requirements_agent_tools/db/requirements.py
  </read_first>
  <action>
    PART A — db/cli.py (17 miss): Add Google-style docstrings to every undocumented
    Click command function and group. The helpers (_emit, _fail, _open_conn,
    _parse_json_list) already have docstrings. Add docstrings to:

      cli (root group):
        """Entry point for the db CLI. Stores project slug on Click context.

        Args:
            ctx: Click context object.
            slug: Project slug matching the directory name under projects/.
        """

      project (group):
        """Project metadata subcommands."""

      project_show:
        """Display the project metadata row as JSON.

        Args:
            ctx: Click context carrying the project slug.
        """

      project_upsert:
        """Insert or update the single project metadata row.

        Args:
            ctx: Click context carrying the project slug.
            name: Human-readable project name.
            code: Optional short project code (e.g. "PROJ-24").
            phase: Project lifecycle phase value.
            objective: One-sentence project objective.
            business_case: Business justification text.
            project_owner: Name of the project owner.
            sponsor: Name of the executive sponsor.
            status_summary: Current status narrative.
        """

      req (group):
        """Requirement CRUD and search subcommands."""

      req_add:
        """Insert a new requirement row.

        Args:
            ctx: Click context carrying the project slug.
            title: Short requirement title.
            created_by: Identifier of the person creating the requirement.
            req_type: Three-letter requirement type code (e.g. "FUN").
            description: Full requirement description text.
            priority: Priority level (low/medium/high/critical).
            owner: Optional owner identifier.
            tags: Comma-separated tag string.
        """

      req_update:
        """Apply a partial field update to an existing requirement.

        Args:
            ctx: Click context carrying the project slug.
            req_id: Requirement identifier to update.
            changed_by: Identifier of the person making the change.
            summary: One-line summary of what changed and why.
            title: New title, if updating.
            description: New description, if updating.
            status: New status value, if updating.
            priority: New priority value, if updating.
            owner: New owner, if updating.
            tags: Comma-separated replacement tag list, if updating.
        """

      req_show:
        """Display a single requirement as JSON.

        Args:
            ctx: Click context carrying the project slug.
            req_id: Requirement identifier to fetch.
        """

      req_search:
        """Search requirements using field-based filters.

        Args:
            ctx: Click context carrying the project slug.
            status: Filter by status value.
            priority: Filter by priority value.
            req_type: Filter by requirement type code.
            owner: Filter by owner identifier.
            tag: Filter by tag string.
            keyword: Filter by keyword substring match in title or description.
        """

      req_vec_search:
        """Run an embedding-based similarity search over requirements.

        Args:
            ctx: Click context carrying the project slug.
            query: Natural-language query string.
            top_k: Number of nearest neighbours to return.
        """

      minute (group):
        """Meeting minute subcommands."""

      minute_add:
        """Log a meeting with its summary, decisions, and action items.

        Args:
            ctx: Click context carrying the project slug.
            title: Meeting title.
            logged_by: Identifier of the person logging the meeting.
            source: Meeting platform (teams/slack/zoom/in-person/other).
            source_url: Optional URL to the meeting recording or transcript.
            occurred_at: ISO datetime string; defaults to current UTC time.
            summary: Short prose summary of the meeting.
            raw_notes: Full verbatim meeting notes.
            attendees: JSON array of attendee name strings.
            decisions: JSON array of Decision dicts.
            action_items: JSON array of ActionItem dicts.
        """

      minute_show:
        """Display one meeting record as JSON.

        Args:
            ctx: Click context carrying the project slug.
            minute_id: Meeting identifier to fetch.
        """

      minute_list:
        """List meetings with optional source, integration, and date filters.

        Args:
            ctx: Click context carrying the project slug.
            source: Filter by meeting source platform.
            unintegrated: If True, return only meetings not yet integrated.
            since: ISO datetime lower bound for occurred_at filter.
        """

      minute_integrate:
        """Mark a meeting as integrated into the project status.

        Args:
            ctx: Click context carrying the project slug.
            minute_id: Meeting identifier to mark as integrated.
        """

      minute_decisions:
        """List decisions extracted across all meetings.

        Args:
            ctx: Click context carrying the project slug.
            status: Filter by decision status.
            affects_req: Filter by affected requirement identifier.
        """

      update (group):
        """Requirement change-log subcommands."""

      update_show:
        """Display the full change history for a requirement.

        Args:
            ctx: Click context carrying the project slug.
            req_id: Requirement identifier whose history to fetch.
        """

    PART B — db/updates.py (1 miss): Add docstring to _row_to_record:
        """Deserialise a DB row dict into an UpdateRecord model instance.

        Args:
            d: Raw row dict from the updates table.

        Returns:
            Populated UpdateRecord with diffs and snapshot deserialised from JSON.
        """

    PART C — Bandit B608 nosec suppressions:
    In db/minutes.py, find the line containing the f-string SQL with {where} (approximately
    line 97). Add the inline comment at the END of that specific line:
        # nosec B608 — where clause built from hardcoded column filter constants; values are parameterized

    In db/requirements.py, find the two flagged lines:
    - The line containing `f"UPDATE requirements SET {', '.join(sets)}"` (~line 201):
        # nosec B608 — sets list built from validated, hardcoded column names; values are parameterized
    - The multi-line f-string SQL with {tag_join} and {where} (~line 284):
        # nosec B608 — tag_join and where built from hardcoded column/table names; values are parameterized

    IMPORTANT: Read each file before editing. Verify bandit finds these exact lines by
    running: uv run --with bandit bandit -r src/requirements_agent_tools/db/minutes.py
    and: uv run --with bandit bandit -r src/requirements_agent_tools/db/requirements.py
    Note the exact line numbers reported, then add the nosec comment to those specific lines.
  </action>
  <verify>
    <automated>uv run --with bandit bandit -r src/requirements_agent_tools/db/ 2>&amp;1 | grep -c "Issue:"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --with bandit bandit -r src/requirements_agent_tools/db/` reports 0 issues
    - `uv run interrogate src/requirements_agent_tools/db/cli.py` shows 100% or near-100% coverage
    - `uv run interrogate src/requirements_agent_tools/db/updates.py` shows 100% coverage
    - All new docstrings follow Google style: summary line, blank line, Args: section with typed params
    - No existing function body was modified — only docstrings added
  </acceptance_criteria>
  <done>
    db/cli.py has docstrings on all 17 previously undocumented Click commands and groups.
    db/updates.py _row_to_record has a docstring.
    bandit reports 0 issues in db/ (B608 false positives suppressed with # nosec B608).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Google-style docstrings to models.py and CONSTANTS.py</name>
  <files>
    src/requirements_agent_tools/models.py
    src/requirements_agent_tools/CONSTANTS.py
  </files>
  <read_first>
    src/requirements_agent_tools/models.py
    src/requirements_agent_tools/CONSTANTS.py
  </read_first>
  <action>
    PART A — models.py (16 miss): Add class-level docstrings to the following classes
    and enums. Do NOT add docstrings to individual enum values — only the class itself.

      RequirementStatus:
        """Lifecycle states for a requirement."""

      RequirementPriority:
        """Priority tiers for triaging and scheduling requirements."""

      RequirementTypeMeta:
        """Metadata tuple binding a type code to its display name and description.

        Attributes:
            code: Three-letter type code matching RequirementType enum values.
            name: Human-readable type name.
            description: One-sentence description of what this type covers.
        """

      ProjectPhase:
        """Lifecycle phases for a project."""

      MeetingSource:
        """Platforms or channels from which a meeting originated."""

      DecisionStatus:
        """Lifecycle states for a decision logged in meeting minutes."""

      ExternalLink:
        """A reference to an external system or resource.

        Attributes:
            system: Name of the external system (e.g. "Jira", "Confluence").
            label: Display label for the link.
            url: Optional URL to the specific item.
        """

      Dependency:
        """A directed dependency between requirements or external systems.

        Attributes:
            kind: Whether the dependency is internal (another requirement)
                or external (outside system).
            ref: Identifier of the dependency target.
            url: Optional URL to the dependency.
            note: Optional explanatory note.
        """

      Stakeholder:
        """A person with a defined role on the project.

        Attributes:
            name: Full name or identifier of the stakeholder.
            role: Stakeholder role (requestor/sponsor/approver/reviewer/informed).
            contact: Optional email or messaging handle.
        """

      FieldDiff:
        """A before/after change record for a single field.

        Attributes:
            field: Name of the changed field.
            old_value: Value before the change.
            new_value: Value after the change.
        """

      UpdateRecord:
        """Audit record capturing a change to a requirement or PROJECT.md.

        Attributes:
            id: UUID for this update record.
            entity_type: Either "requirement" or "project_md".
            entity_id: Identifier of the changed entity.
            changed_at: Timestamp of the change (UTC).
            changed_by: Identifier of the person who made the change.
            summary: One-line description of what changed and why.
            diffs: List of per-field before/after diffs.
            full_snapshot: Optional full row snapshot for status transitions.
        """

      Decision:
        """A decision recorded during a meeting.

        Attributes:
            decision_id: Unique decision identifier (auto-generated DEC-XXXXXXXX).
            title: Short title summarising the decision.
            detail: Full decision text.
            made_by: List of participant identifiers who made the decision.
            status: Current decision status.
            affects_reqs: Requirement identifiers affected by this decision.
            action_owner: Optional identifier of who owns the follow-up action.
            due_date: Optional date by which the action should be completed.
            notes: Freeform notes or follow-up context.
        """

      ActionItem:
        """A follow-up action item arising from a meeting decision.

        Attributes:
            action_id: Unique action identifier (auto-generated ACT-XXXXXXXX).
            description: Full description of the action to take.
            owner: Optional identifier of the person responsible.
            due_date: Optional completion date.
            done: True when the action has been completed.
        """

      MinuteIn:
        """Validated input for logging a meeting.

        Attributes:
            title: Meeting title.
            source: Platform or channel where the meeting occurred.
            source_url: Optional URL to the meeting recording or notes.
            occurred_at: When the meeting took place (UTC).
            logged_by: Identifier of the person logging the minutes.
            attendees: List of attendee names or identifiers.
            summary: Short prose summary of the meeting.
            raw_notes: Full verbatim notes.
            decisions: Decisions made during the meeting.
            action_items: Action items arising from the meeting.
        """

      MinuteRow:
        """Full DB row — extends MinuteIn with persistence fields.

        Attributes:
            id: UUID for this meeting record.
            logged_at: When the minutes were persisted (UTC).
            integrated_into_status: True when the meeting has been integrated
                into the project status summary.
            integrated_at: Timestamp of integration, if integrated.
        """

      ProjectMeta:
        """Full project metadata record.

        Attributes:
            project_id: UUID for the project.
            slug: URL-safe slug derived from the project name.
            name: Human-readable project name.
            code: Optional short project code (e.g. "PROJ-24").
            phase: Current project lifecycle phase.
            objective: One-sentence project objective.
            business_case: Business justification narrative.
            success_criteria: List of measurable success criteria statements.
            out_of_scope: List of explicitly out-of-scope items.
            project_owner: Name or identifier of the project owner.
            sponsor: Name or identifier of the executive sponsor.
            stakeholders: List of project stakeholders with roles.
            start_date: Planned or actual project start date.
            target_date: Target completion date.
            actual_end_date: Actual completion date, if closed.
            external_links: Links to external systems (e.g. Jira, Confluence).
            status_summary: Current status narrative for the project.
            status_updated_at: When the status summary was last updated.
            created_at: When the project record was created (UTC).
            updated_at: When the project record was last modified (UTC).
        """

    PART B — CONSTANTS.py (3 miss): Add docstrings to db_path, md_path, and slugify.

      db_path:
        """Return the SQLite database file path for a given project slug.

        Args:
            slug: Project slug (directory name under PROJECTS_DIR).

        Returns:
            Path to the .db file at PROJECTS_DIR/<slug>/<slug>.db.
        """

      md_path:
        """Return the PROJECT.md path for a given project slug.

        Args:
            slug: Project slug (directory name under PROJECTS_DIR).

        Returns:
            Path to PROJECT.md at PROJECTS_DIR/<slug>/PROJECT.md.
        """

      slugify:
        """Convert a human-readable name into a URL-safe slug.

        Replaces any sequence of non-alphanumeric characters with a single
        hyphen and strips leading/trailing hyphens.

        Args:
            name: Human-readable name to slugify.

        Returns:
            Lowercase hyphen-separated slug string.
        """

    Style rule: Every docstring must start with a one-sentence summary on the same line as
    the opening triple-quote. Args/Returns/Attributes sections use 4-space indented items.
  </action>
  <verify>
    <automated>uv run interrogate src/requirements_agent_tools/models.py src/requirements_agent_tools/CONSTANTS.py -v 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `uv run interrogate src/requirements_agent_tools/models.py` shows >= 90% coverage
    - `uv run interrogate src/requirements_agent_tools/CONSTANTS.py` shows 100% coverage
    - No existing class field, method body, or validator was modified
    - All 16 previously undocumented classes/enums in models.py now have class-level docstrings
    - All 3 previously undocumented functions in CONSTANTS.py now have Google-style docstrings
  </acceptance_criteria>
  <done>
    models.py has class-level docstrings on RequirementStatus, RequirementPriority,
    RequirementTypeMeta, ProjectPhase, MeetingSource, DecisionStatus, ExternalLink,
    Dependency, Stakeholder, FieldDiff, UpdateRecord, Decision, ActionItem, MinuteIn,
    MinuteRow, ProjectMeta.
    CONSTANTS.py has docstrings on db_path, md_path, slugify.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Google-style docstrings to the 6 CLI modules and verify 90% coverage</name>
  <files>
    src/requirements_agent_tools/init_project.py
    src/requirements_agent_tools/req_ops.py
    src/requirements_agent_tools/meeting.py
    src/requirements_agent_tools/report.py
    src/requirements_agent_tools/review.py
    src/requirements_agent_tools/project_md_cli.py
  </files>
  <read_first>
    src/requirements_agent_tools/init_project.py
    src/requirements_agent_tools/req_ops.py
    src/requirements_agent_tools/meeting.py
    src/requirements_agent_tools/report.py
    src/requirements_agent_tools/review.py
    src/requirements_agent_tools/project_md_cli.py
  </read_first>
  <action>
    For each module, add Google-style docstrings to EVERY function that lacks one.
    refine.py already has docstrings on all 4 cmd_* functions — skip it.

    ── init_project.py (5 miss: cmd_new, cmd_list, cmd_update, build_parser, main) ──

      cmd_new:
        """Create a new project database and directory.

        Validates that no project with the same slug already exists, then
        creates the DB, writes project metadata, and prints a JSON result
        with the project ID, slug, and next-step instructions.

        Args:
            args: Parsed CLI arguments from build_parser().
        """

      cmd_list:
        """List all existing projects found in PROJECTS_DIR.

        Args:
            args: Parsed CLI arguments from build_parser().
        """

      cmd_update:
        """Update metadata fields for an existing project.

        Resolves the project by slug or name, applies all non-None CLI
        argument values to the project metadata, and persists the change.

        Args:
            args: Parsed CLI arguments from build_parser().
        """

      build_parser:
        """Build and return the init-project argument parser.

        Returns:
            Configured ArgumentParser with new, list, and update subcommands.
        """

      main:
        """Entry point for the init-project CLI."""

    ── req_ops.py (9 miss: cmd_add, cmd_update, cmd_get, cmd_list, cmd_search,
                           cmd_history, cmd_vector, build_parser, main) ──

      cmd_add:
        """Add a new requirement to the active project.

        Args:
            args: Parsed CLI arguments from build_parser().
        """

      cmd_update:
        """Apply a partial field update to an existing requirement.

        Args:
            args: Parsed CLI arguments from build_parser().
        """

      cmd_get:
        """Fetch and display a single requirement by ID.

        Args:
            args: Parsed CLI arguments from build_parser().
        """

      cmd_list:
        """List all requirements with optional field filters.

        Args:
            args: Parsed CLI arguments from build_parser().
        """

      cmd_search:
        """Keyword search across requirement titles and descriptions.

        Args:
            args: Parsed CLI arguments from build_parser().
        """

      cmd_history:
        """Display the full change history for a requirement.

        Args:
            args: Parsed CLI arguments from build_parser().
        """

      cmd_vector:
        """Embedding-based similarity search over requirements.

        Requires EMBEDDING_API_KEY environment variable to be set.

        Args:
            args: Parsed CLI arguments from build_parser().
        """

      build_parser:
        """Build and return the req-ops argument parser.

        Returns:
            Configured ArgumentParser with add, update, get, list, search,
            history, and vector subcommands.
        """

      main:
        """Entry point for the req-ops CLI."""

    ── meeting.py (9 miss: cmd_log, cmd_get, cmd_list, cmd_decisions,
                           cmd_update_decision, cmd_close_action, cmd_integrate,
                           build_parser, main) ──

      cmd_log:
        """Log a meeting with its title, summary, decisions, and action items.

        Args:
            args: Parsed CLI arguments from build_parser().
        """

      cmd_get:
        """Fetch and display a single meeting record.

        Args:
            args: Parsed CLI arguments from build_parser().
        """

      cmd_list:
        """List meetings with optional source, integration, and date filters.

        Args:
            args: Parsed CLI arguments from build_parser().
        """

      cmd_decisions:
        """List all decisions across all meeting records.

        Args:
            args: Parsed CLI arguments from build_parser().
        """

      cmd_update_decision:
        """Update the status and notes of a specific decision.

        Args:
            args: Parsed CLI arguments from build_parser().
        """

      cmd_close_action:
        """Mark a specific action item as done.

        Args:
            args: Parsed CLI arguments from build_parser().
        """

      cmd_integrate:
        """Mark one or more meetings as integrated and optionally update project status.

        Args:
            args: Parsed CLI arguments from build_parser().
        """

      build_parser:
        """Build and return the meeting argument parser.

        Returns:
            Configured ArgumentParser with log, get, list, decisions,
            update_decision, close_action, and integrate subcommands.
        """

      main:
        """Entry point for the meeting CLI."""

    ── report.py (6 miss: _build_report, _report_to_md, cmd_generate, cmd_save,
                          build_parser, main) ──

      _build_report:
        """Assemble the full status report dict from the project DB.

        Queries requirements, meetings, decisions, action items, and recent
        changes to produce a structured report dict suitable for JSON output
        or Markdown rendering.

        Args:
            slug: Project slug (used for file path construction).
            conn: Open SQLite connection to the project DB.
            meta: ProjectMeta instance for the project.

        Returns:
            Dict containing project metadata, requirement counts, FRET
            coverage, meeting/decision/action summaries, and a health signal.
        """

      _report_to_md:
        """Render a report dict as a Markdown status report string.

        Args:
            report: Report dict produced by _build_report().

        Returns:
            Multi-line Markdown string suitable for writing to a .md file.
        """

      cmd_generate:
        """Generate a project status report and print it to stdout.

        Args:
            args: Parsed CLI arguments from build_parser().
        """

      cmd_save:
        """Generate a project status report and save it to timestamped files.

        Writes both a Markdown file (STATUS-<timestamp>.md) and a JSON file
        (STATUS-<timestamp>.json) to the project directory.

        Args:
            args: Parsed CLI arguments from build_parser().
        """

      build_parser:
        """Build and return the report argument parser.

        Returns:
            Configured ArgumentParser with generate and save subcommands.
        """

      main:
        """Entry point for the report CLI."""

    ── review.py (8 miss: _check_gaps, cmd_gaps, cmd_conflicts, cmd_coverage,
                          cmd_report, build_parser, main, _check_coverage) ──

      _check_gaps:
        """Identify gaps in requirement coverage across type, fields, and FRET.

        Flags: missing requirement types, requirements without descriptions,
        requirements without an owner, requirements without a FRET statement,
        and open critical requirements.

        Args:
            reqs: List of RequirementRow objects from the active project.
            meta: ProjectMeta for the active project.

        Returns:
            Dict with gap_count (int) and gaps (list of issue dicts).
        """

      _check_coverage:
        """Cross-check project success criteria against existing requirements.

        Extracts keywords from each criterion and checks whether any
        requirement's title, description, or FRET statement contains those
        keywords.

        Args:
            reqs: List of RequirementRow objects from the active project.
            meta: ProjectMeta containing the success_criteria list.

        Returns:
            Dict with criteria_total, criteria_covered, uncovered list,
            and per-criterion detail. Returns a skipped dict if no criteria
            are defined.
        """

      cmd_gaps:
        """Run gap analysis and output identified issues as JSON.

        Args:
            args: Parsed CLI arguments from build_parser().
        """

      cmd_conflicts:
        """Run conflict detection and output flagged conflicts as JSON.

        Args:
            args: Parsed CLI arguments from build_parser().
        """

      cmd_coverage:
        """Run success criteria coverage check and output results as JSON.

        Args:
            args: Parsed CLI arguments from build_parser().
        """

      cmd_report:
        """Run all review checks and output a combined review report as JSON.

        Args:
            args: Parsed CLI arguments from build_parser().
        """

      build_parser:
        """Build and return the review argument parser.

        Returns:
            Configured ArgumentParser with gaps, conflicts, coverage,
            and report subcommands.
        """

      main:
        """Entry point for the review CLI."""

    ── project_md_cli.py (6 miss: _resolve_content, cmd_save, cmd_append, cmd_read,
                                   build_parser, main) ──

      _resolve_content:
        """Resolve inline content or a file path to a string.

        Exactly one of inline or file_path must be provided; providing both
        or neither causes an error exit.

        Args:
            inline: Inline content string, or None.
            file_path: Path to a file to read, or None.
            label: CLI option name used in error messages (e.g. "content").

        Returns:
            The resolved content string.
        """

      cmd_save:
        """Write the full PROJECT.md content, creating or replacing the file.

        Args:
            args: Parsed CLI arguments from build_parser().
        """

      cmd_append:
        """Append a markdown section to the existing PROJECT.md.

        Args:
            args: Parsed CLI arguments from build_parser().
        """

      cmd_read:
        """Print the current PROJECT.md content to stdout.

        Args:
            args: Parsed CLI arguments from build_parser().
        """

      build_parser:
        """Build and return the project-md-cli argument parser.

        Returns:
            Configured ArgumentParser with save, append, and read subcommands.
        """

      main:
        """Entry point for the project_md_cli CLI."""

    After adding all docstrings, run the coverage check to confirm >= 90%:
      uv run interrogate src/ --fail-under 90

    If coverage is still below 90%, check which files still have misses:
      uv run interrogate src/ -v
    Then add the missing docstrings until the threshold is reached.
  </action>
  <verify>
    <automated>uv run interrogate src/ --fail-under 90</automated>
  </verify>
  <acceptance_criteria>
    - `uv run interrogate src/ --fail-under 90` exits 0 (coverage >= 90%)
    - `uv run interrogate src/ -v` shows >= 150/166 items documented
    - All 6 modules have docstrings on every function listed above
    - Docstrings follow Google style (one-line summary + Args/Returns sections)
    - No function body, argument, or return value was changed — only docstrings added
  </acceptance_criteria>
  <done>
    All 6 CLI modules (init_project, req_ops, meeting, report, review, project_md_cli)
    have Google-style docstrings on every previously undocumented function.
    `uv run interrogate src/ --fail-under 90` exits 0.
  </done>
</task>

<!-- ═══════════════════════════════════════════════════════════════════
     WAVE B: Create .pre-commit-config.yaml (Task 4)
     Must run only after Tasks 1–3 pass interrogate at 90%.
     ═══════════════════════════════════════════════════════════════════ -->

<task type="auto">
  <name>Task 4: Create .pre-commit-config.yaml and verify all hooks pass</name>
  <files>
    .pre-commit-config.yaml
  </files>
  <read_first>
    pyproject.toml
  </read_first>
  <action>
    PREREQUISITE CHECK: Before creating .pre-commit-config.yaml, verify:
      uv run interrogate src/ --fail-under 90
    If this exits non-zero, STOP and complete Tasks 1–3 first.

    Step 1: Create .pre-commit-config.yaml with this EXACT content:

    ---
    repos:
      # ── Formatting (must run before lint) ──────────────────────────────────
      - repo: https://github.com/astral-sh/ruff-pre-commit
        rev: v0.15.12
        hooks:
          - id: ruff-format
          - id: ruff-check
            args: [--fix]

      # ── Credentials ────────────────────────────────────────────────────────
      - repo: https://github.com/pre-commit/pre-commit-hooks
        rev: v6.0.0
        hooks:
          - id: detect-private-key

      # ── Security ───────────────────────────────────────────────────────────
      - repo: https://github.com/PyCQA/bandit
        rev: 1.9.4
        hooks:
          - id: bandit
            args: [-r, src/]
            pass_filenames: false

      # ── Type checking (local — no astral-sh/ty-pre-commit repo exists) ─────
      - repo: local
        hooks:
          - id: ty
            name: ty type check
            entry: uv run --with ty ty check
            language: system
            types: [python]
            pass_filenames: false

      # ── Docstring coverage ─────────────────────────────────────────────────
      - repo: https://github.com/econchick/interrogate
        rev: 1.7.0
        hooks:
          - id: interrogate
            pass_filenames: false

      # ── Documentation regeneration (lazydocs → docs/) ─────────────────────
      - repo: local
        hooks:
          - id: lazydocs
            name: regenerate API docs (lazydocs)
            entry: uv run --with lazydocs lazydocs --output-path docs/ requirements_agent_tools
            language: system
            pass_filenames: false
            always_run: true

      # ── Tests ──────────────────────────────────────────────────────────────
      - repo: local
        hooks:
          - id: pytest
            name: pytest
            entry: uv run pytest
            language: system
            pass_filenames: false
            always_run: true

    Step 2: Install the pre-commit hook runner:
      uv run pre-commit install

    Step 3: Run all hooks against all files and confirm every hook passes:
      uv run pre-commit run --all-files

    If any hook fails:
    - ruff-format or ruff-check: fix the formatting/lint issue, then re-run
    - bandit: verify the # nosec B608 comments were added in Task 1; re-check
              uv run --with bandit bandit -r src/ for remaining issues
    - ty: check [tool.ty.rules] invalid-argument-type = "warn" is in pyproject.toml
    - interrogate: coverage is below 90% — add more docstrings then re-run
    - lazydocs: ensure the command `uv run --with lazydocs lazydocs --output-path docs/
                requirements_agent_tools` works standalone
    - pytest: a test is failing — diagnose and fix

    IMPORTANT NOTE on lazydocs staging: The lazydocs hook regenerates docs/ on every
    commit, but newly generated files are NOT automatically staged by pre-commit.
    Before committing, run:
      uv run --with lazydocs lazydocs --output-path docs/ requirements_agent_tools
      git add docs/
    Document this in CLAUDE.md (handled by Plan 04).
  </action>
  <verify>
    <automated>uv run pre-commit run --all-files</automated>
  </verify>
  <acceptance_criteria>
    - `.pre-commit-config.yaml` exists at the repo root
    - `grep -c "ruff-format" .pre-commit-config.yaml` returns 1
    - `grep -c "detect-private-key" .pre-commit-config.yaml` returns 1
    - `grep -c "bandit" .pre-commit-config.yaml` returns at least 1
    - `grep -c "interrogate" .pre-commit-config.yaml` returns at least 1
    - `grep -c "lazydocs" .pre-commit-config.yaml` returns at least 1
    - `grep -c "pytest" .pre-commit-config.yaml` returns at least 1
    - `uv run pre-commit run --all-files` exits 0 (all hooks pass)
    - `uv run interrogate src/ --fail-under 90` exits 0
    - `uv run --with bandit bandit -r src/` exits 0 (0 issues)
  </acceptance_criteria>
  <done>
    .pre-commit-config.yaml exists with 8 hooks (ruff-format, ruff-check,
    detect-private-key, bandit, ty, interrogate, lazydocs, pytest).
    `uv run pre-commit run --all-files` exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → pre-commit hooks | Hook scripts run arbitrary commands in the developer's shell on every commit |
| bandit nosec suppressions | Suppressing security warnings removes automated detection from those lines |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00.5-03-01 | Tampering | bandit nosec suppressions | mitigate | Each # nosec B608 comment includes a justification; all 3 suppressions are confirmed false positives (column names hardcoded, values parameterized); document in code review checklist |
| T-00.5-03-02 | Denial of Service | pytest pre-commit hook | accept | Hook runs full test suite on every commit; acceptable for solo developer workflow; tests already run in ~5s |
| T-00.5-03-03 | Information Disclosure | detect-private-key hook | mitigate | Hook blocks commits containing private key patterns; reduces accidental credential exposure |
</threat_model>

<verification>
Run these commands after plan completion:

```bash
# Coverage gate
uv run interrogate src/ --fail-under 90

# Security scan clean
uv run --with bandit bandit -r src/

# All hooks pass
uv run pre-commit run --all-files

# Hook file structure check
grep -c "ruff-format" .pre-commit-config.yaml
grep -c "interrogate" .pre-commit-config.yaml
grep -c "pytest" .pre-commit-config.yaml
```

All commands must exit 0.
</verification>

<success_criteria>
1. `uv run interrogate src/ --fail-under 90` exits 0 (>= 90% docstring coverage)
2. `.pre-commit-config.yaml` exists with all 8 hooks
3. `uv run pre-commit run --all-files` exits 0 (every hook passes)
4. `uv run --with bandit bandit -r src/` reports 0 issues
5. All new docstrings use Google style (summary + Args/Returns sections)
</success_criteria>

<output>
After completion, create `.planning/phases/00.5-package-scaffold/00.5-03-SUMMARY.md`
following the template at @/home/aleksander/.claude/get-shit-done/templates/summary.md.
</output>
