---
phase: 00.5-package-scaffold
plan: 04
type: execute
wave: 4
depends_on: ["00.5-03"]
files_modified:
  - CLAUDE.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [PKG-05, PKG-06]

must_haves:
  truths:
    - "CLAUDE.md contains no reference to shared/ in the architecture section"
    - "CLAUDE.md contains no reference to skills/refine-requirements/scripts/refine.py"
    - "CLAUDE.md Known Issues section is removed entirely"
    - "CLAUDE.md has a Package (src/requirements_agent_tools/) section with the entry-point table"
    - "CLAUDE.md Commands section includes doc-gen and pre-commit commands"
    - "CLAUDE.md has a Gitagent Fallback Pattern subsection under Agent Configuration"
    - "CLAUDE.md Skill Development Convention step 2 references src/requirements_agent_tools/ not skills/<name>/scripts/"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Accurate developer guide for the post-scaffold package structure"
      contains: "src/requirements_agent_tools"
  key_links:
    - from: "CLAUDE.md Package section"
      to: "pyproject.toml [project.scripts]"
      via: "entry-point table matches pyproject.toml script names exactly"
      pattern: "uv run init-project"
    - from: "CLAUDE.md Gitagent Fallback Pattern"
      to: "agent.yaml model.fallback"
      via: "documents the fallback list from agent.yaml"
      pattern: "claude-opus-4.6"
---

<objective>
Rewrite CLAUDE.md to accurately describe the post-scaffold repository: replace the
shared/ section with a src/requirements_agent_tools/ package section, add entry-point
command examples, document the gitagent fallback pattern, remove the fixed Known Issues
section, and update the Skill Development Convention.

Purpose: CLAUDE.md is the primary developer reference read by both humans and the Claude
Code agent. It currently references the deleted shared/ directory and a deleted
skills/refine-requirements/scripts/refine.py script. These stale references will mislead
the agent about the CLI surface. Per D-05, D-07, D-08.

Output: A single updated CLAUDE.md file reflecting the current package structure and
toolchain.
</objective>

<execution_context>
@/home/aleksander/.claude/get-shit-done/workflows/execute-plan.md
@/home/aleksander/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00.5-package-scaffold/00.5-RESEARCH.md

<interfaces>
<!-- Current CLAUDE.md structure (read 2026-04-29) -->
<!-- Lines 1–81 of CLAUDE.md — full file shown for reference -->
<!--
  Line 1:   # CLAUDE.md
  Line 3:   This file provides guidance...
  Lines 9–25:  ## Commands (uv sync, ruff check, ruff format, pytest, single test)
  Lines 27–29: ## Architecture / ### Gitagent
  Lines 32–35: ### Agent Configuration (agent.yaml, claude-opus-4.6)
  Lines 37–46: ### Skills (skills/) — line 38 has stale "scripts/" reference
  Line 41:  "has a CLI at skills/refine-requirements/scripts/refine.py" — STALE
  Lines 48–57: ### Shared Library (shared/) — ENTIRE SECTION IS STALE
  Lines 59–66: ### Supporting Directories — KEEP AS-IS
  Lines 68–69: ### Data Layer — line 69 says "The refine.py CLI" — STALE
  Lines 71–73: ## Known Issues — REMOVE ENTIRELY (D-07)
  Lines 75–81: ## Skill Development Convention — step 2 is stale

  Verified stale references (from RESEARCH.md Finding 6):
  | Line | Old Content | New Content |
  |------|-------------|-------------|
  | 38   | "optionally `scripts/` and `references/`" | "optionally `references/`. CLI logic for a skill lives in `src/requirements_agent_tools/<module>.py` and is wired as a `[project.scripts]` entry point in `pyproject.toml`." |
  | 41   | "has a CLI at `skills/refine-requirements/scripts/refine.py`" | "invoked via `uv run refine`" |
  | 48–57 | Entire "### Shared Library (`shared/`)" section | "### Package (`src/requirements_agent_tools/`)" section (see Task 1 below) |
  | 69   | "The `refine.py` CLI outputs" | "The `uv run refine` CLI outputs" |
  | 71–73 | ## Known Issues section | REMOVE ENTIRELY |
  | 79   | "Place CLI scripts under `skills/<name>/scripts/` and reference material under `skills/<name>/references/`." | "Place reference material under `skills/<name>/references/`. CLI logic goes in `src/requirements_agent_tools/<name>.py` and is wired as a `[project.scripts]` entry point in `pyproject.toml`." |

  Agent configuration (from agent.yaml verified 2026-04-29):
    model:
      preferred: anthropic/claude-opus-4.6
      fallback:
        - openai/gpt-5.2
        - openai/gpt-oss-20b
        - google/gemma-4-31b
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite CLAUDE.md with accurate package structure and remove stale content</name>
  <files>CLAUDE.md</files>
  <read_first>
    CLAUDE.md
    agent.yaml
    pyproject.toml
  </read_first>
  <action>
    Read CLAUDE.md in full, then write the complete replacement file with ALL of the
    following changes applied. Write the entire file, not just the changed sections.

    CHANGE 1 — ## Commands section: Add three new command blocks after the existing
    "Run a single test" block. The final Commands section must read:

    ```
    ## Commands

    ```bash
    # Install dependencies
    uv sync

    # Run linter
    uv run ruff check .
    uv run ruff format .

    # Run tests
    uv run pytest

    # Run a single test
    uv run pytest path/to/test_file.py::test_name

    # Generate API docs (lazydocs → docs/)
    uv run --with lazydocs lazydocs --output-path docs/ requirements_agent_tools
    # Note: run `git add docs/` after this if committing docs changes

    # Install pre-commit hooks (one-time setup per clone)
    uv run pre-commit install

    # Run all pre-commit hooks manually
    uv run pre-commit run --all-files
    ```
    ```

    CHANGE 2 — ### Agent Configuration section: After the existing paragraph about
    agent.yaml, add a new subsection:

    ```
    #### Gitagent Fallback Pattern

    The gitagent harness reads `agent.yaml` and selects the next model on failure. Current
    fallback list:
    1. `anthropic/claude-opus-4.6` (preferred)
    2. `openai/gpt-5.2`
    3. `openai/gpt-oss-20b`
    4. `google/gemma-4-31b`

    If the preferred model is unavailable or returns an error, the harness automatically
    retries with the next entry in the list. No code change is required to change the
    fallback order — edit `agent.yaml` under `model.fallback`.

    **Note:** The fallback model IDs above reflect the current `agent.yaml` configuration
    but may not correspond to currently available production models. Verify availability
    against your provider contracts before production use.
    ```

    CHANGE 3 — ### Skills (`skills/`) section: Replace the two stale lines.

    Old line 38:
      "Each skill is a directory with `SKILL.md` (frontmatter + instructions) and optionally `scripts/` and `references/`. The six skills are:"

    New line 38:
      "Each skill is a directory with `SKILL.md` (frontmatter + instructions) and optionally `references/`. CLI logic for a skill lives in `src/requirements_agent_tools/<module>.py` and is wired as a `[project.scripts]` entry point in `pyproject.toml`. The six skills are:"

    Old line 41:
      "- **refine-requirements** — applies FRET grammar interactively; has a CLI at `skills/refine-requirements/scripts/refine.py` and full grammar reference at `skills/refine-requirements/references/fret_grammar.md`"

    New line 41:
      "- **refine-requirements** — applies FRET grammar interactively; invoked via `uv run refine`; full grammar reference at `skills/refine-requirements/references/fret_grammar.md`"

    CHANGE 4 — ### Shared Library (`shared/`) section: REPLACE THE ENTIRE SECTION
    (from "### Shared Library (`shared/`)" through the last bullet about project_session.py)
    with the following new section:

    ```
    ### Package (`src/requirements_agent_tools/`)

    All logic lives in the installed `requirements_agent_tools` package. Import directly
    from the submodule you need. The seven entry points are:

    | Entry point | Module | Subcommands |
    |-------------|--------|-------------|
    | `uv run init-project` | `init_project:main` | `new`, `list`, `update` |
    | `uv run refine` | `refine:main` | `pending`, `show`, `apply`, `coverage` |
    | `uv run req-ops` | `req_ops:main` | `add`, `update`, `get`, `list`, `search`, `history`, `vector` |
    | `uv run review` | `review:main` | `gaps`, `conflicts`, `coverage`, `report` |
    | `uv run report` | `report:main` | `generate`, `save` |
    | `uv run meeting` | `meeting:main` | `log`, `get`, `list`, `decisions`, `update_decision`, `close_action`, `integrate` |
    | `uv run db` | `db.cli:cli` | `req`, `project`, `minute`, `update` |

    `--project <slug>` is an optional global flag on `refine`, `req-ops`, `review`, `report`,
    `meeting`. On `db` it is a required option (`--project SLUG` before the subcommand).

    Key modules:

    - **`CONSTANTS.py`** — single source of truth for paths (`PROJECTS_DIR`), embedding config,
      and path helper functions (`project_dir`, `db_path`, `md_path`, `slugify`)
    - **`models.py`** — Pydantic validation models (`RequirementIn`, `RequirementRow`,
      `ProjectMeta`, `MinuteIn`, `UpdateRecord`, etc.); nothing writes to the DB without going
      through these first
    - **`db/`** — SQLite persistence layer split into focused submodules: `connection`
      (open/bootstrap), `schema` (DDL + reference seeds), `projects`, `requirements`, `minutes`,
      `updates`, `embeddings` (sqlite-vec), plus private `_serialization` and `_logging`
      helpers. All log calls go through loguru — stderr (level via `REQ_AGENT_LOG_LEVEL`,
      default `INFO`) plus a per-project daily-rotated file at
      `projects/<slug>/logs/db-{date}.log` at `DEBUG`. Import directly from the submodule you
      need (`from requirements_agent_tools.db.requirements import insert_requirement`); the
      package does not re-export.
    - **`project_md.py`** — generates and maintains `PROJECT.md` (auto header + protected
      `<!-- NOTES:BEGIN/END -->` block)
    - **`project_session.py`** — `resolve(slug_or_name)` auto-selects the active project;
      `refresh_md()` should be called after every write
    ```

    CHANGE 5 — ### Data Layer section: Replace stale reference.

    Old: "The `refine.py` CLI outputs structured JSON to stdout"
    New: "The `uv run refine` CLI outputs structured JSON to stdout"

    CHANGE 6 — ## Known Issues section: REMOVE ENTIRELY.
    Delete the entire section including the heading and its single bullet point.
    The section to remove is:
      ## Known Issues

      - `skills/status-report/scripts/report.py.py` has a double `.py` extension
        (root-owned file — rename to `report.py` when possible). The `SKILL.md` and
        README both reference it correctly as `report.py`.

    CHANGE 7 — ## Skill Development Convention step 2: Replace the old step text.

    Old step 2:
      "2. Place CLI scripts under `skills/<name>/scripts/` and reference material under `skills/<name>/references/`."

    New step 2:
      "2. Place reference material under `skills/<name>/references/`. CLI logic goes in `src/requirements_agent_tools/<name>.py` and is wired as a `[project.scripts]` entry point in `pyproject.toml`."

    WRITE THE COMPLETE FILE: Produce the full CLAUDE.md content with all 7 changes
    applied simultaneously. Do not produce a diff — write the entire file.
  </action>
  <verify>
    <automated>grep -q "src/requirements_agent_tools" CLAUDE.md && test $(grep -c "shared/" CLAUDE.md) -eq 0 && ! grep -q "Known Issues" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "src/requirements_agent_tools" CLAUDE.md` returns >= 3 (section header,
      entry-point table reference, step 2 of Skill Development Convention)
    - `grep -c "shared/" CLAUDE.md` returns 0 (no remaining shared/ references in
      the architecture section; the phrase appears only in comments if any)
    - `grep "scripts/refine.py" CLAUDE.md` returns empty (stale path removed)
    - `grep "Known Issues" CLAUDE.md` returns empty (section removed)
    - `grep -c "Gitagent Fallback Pattern" CLAUDE.md` returns 1 (new subsection added)
    - `grep -c "uv run pre-commit install" CLAUDE.md` returns 1 (new command added)
    - `grep -c "uv run --with lazydocs" CLAUDE.md` returns 1 (new doc-gen command added)
    - `grep "uv run refine" CLAUDE.md` returns a match in the Skills section
    - `grep "uv run init-project" CLAUDE.md` returns a match in the entry-point table
  </acceptance_criteria>
  <done>
    CLAUDE.md updated: shared/ section replaced with src/requirements_agent_tools/ package
    section including entry-point table, Commands section has doc-gen and pre-commit
    commands, Agent Configuration has Gitagent Fallback Pattern subsection, Known Issues
    section removed, Skill Development Convention step 2 updated, Data Layer updated to
    say "uv run refine".
  </done>
</task>


<task type="auto">
  <name>Task 2: Update ROADMAP.md Phase 0.5 success criteria to reflect lazydocs (D-09)</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    .planning/ROADMAP.md
    .planning/phases/00.5-package-scaffold/00.5-CONTEXT.md
  </read_first>
  <action>
    Read ROADMAP.md in full, then apply these exact text replacements:

    CHANGE A — SC3: Replace the old success criterion text:
      Old: "docs/requirements_agent_tools/index.html exists and docs/requirements_agent_tools.md exists (pdoc HTML + Markdown output committed)"
      New: "docs/requirements_agent_tools.md exists (lazydocs Markdown API reference committed, 21 files)"

    CHANGE B — SC4 hook list: Replace the old hook list:
      Old: "ruff-format, ruff-check, ty, bandit, detect-private-key, interrogate (≥90%), pdoc, pytest"
      New: "ruff-format, ruff-check, ty, bandit, detect-private-key, interrogate (≥90%), lazydocs, pytest"

    CHANGE C — Phase 0.5 goal statement: If the goal line contains "pdoc", replace that
    occurrence with "lazydocs". If "pdoc" does not appear in the goal, skip this change.

    Write the complete updated ROADMAP.md. Do not produce a diff — write the entire file.
  </action>
  <verify>
    <automated>grep -q lazydocs .planning/ROADMAP.md && ! grep -q pdoc .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    -  exits 0 (lazydocs present)
    -  exits 0 (no remaining pdoc references)
    - SC3 references lazydocs Markdown output, not pdoc HTML
    - SC4 hook list contains lazydocs, not pdoc
  </acceptance_criteria>
  <done>
    ROADMAP.md Phase 0.5 success criteria updated: SC3 reflects lazydocs Markdown output
    (21 files), SC4 hook list uses lazydocs in place of pdoc. No remaining pdoc references
    anywhere in the Phase 0.5 section.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLAUDE.md → Claude Code agent | CLAUDE.md is read by the agent as its primary project instruction file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00.5-04-01 | Information Disclosure | CLAUDE.md agent.yaml fallback model list | accept | Fallback model IDs are in agent.yaml which is a committed config file; documenting them in CLAUDE.md adds no new exposure. Note added that IDs may not reflect current production models. |
| T-00.5-04-02 | Tampering | CLAUDE.md content accuracy | mitigate | Acceptance criteria include grep checks that verify all 7 changes applied correctly and no stale references remain |
</threat_model>

<verification>
Run these commands after plan completion:

```bash
# No stale shared/ references
grep -c "shared/" CLAUDE.md

# No stale scripts/refine.py reference
grep "scripts/refine.py" CLAUDE.md

# Known Issues removed
grep "Known Issues" CLAUDE.md

# New content present
grep -c "src/requirements_agent_tools" CLAUDE.md
grep -c "Gitagent Fallback Pattern" CLAUDE.md
grep -c "uv run pre-commit install" CLAUDE.md
```

`grep -c "shared/" CLAUDE.md` must return 0.
`grep -c "src/requirements_agent_tools" CLAUDE.md` must return >= 3.
All other grep checks for removed content must return empty output.
</verification>

<success_criteria>
1. CLAUDE.md contains no references to `shared/` in the architecture section
2. CLAUDE.md contains no reference to `skills/refine-requirements/scripts/refine.py`
3. `## Known Issues` section is absent from CLAUDE.md
4. CLAUDE.md `### Package (src/requirements_agent_tools/)` section exists with the 7-row entry-point table
5. CLAUDE.md `## Commands` section includes `uv run pre-commit install` and `uv run --with lazydocs lazydocs`
6. CLAUDE.md `#### Gitagent Fallback Pattern` subsection exists under `### Agent Configuration`
7. CLAUDE.md Skill Development Convention step 2 references `src/requirements_agent_tools/<name>.py`
</success_criteria>

<output>
After completion, create `.planning/phases/00.5-package-scaffold/00.5-04-SUMMARY.md`
following the template at @/home/aleksander/.claude/get-shit-done/templates/summary.md.
</output>
