---
phase: 00.5-package-scaffold
verified: 2026-04-28T14:30:00Z
status: gaps_found
score: 3/6 must-haves verified
overrides_applied: 0
gaps:
  - truth: "docs/requirements_agent_tools/index.html exists and docs/requirements_agent_tools.md exists (pdoc HTML + Markdown output committed)"
    status: partial
    reason: "ROADMAP SC3 requires both pdoc HTML output at docs/requirements_agent_tools/index.html AND Markdown. The decision in CONTEXT.md D-09 switched from pdoc to lazydocs (Markdown-only), but ROADMAP SC3 text was never updated to reflect this. docs/requirements_agent_tools.md exists (21 Markdown files total) but docs/requirements_agent_tools/index.html does NOT exist. The pdoc HTML requirement in the ROADMAP contract is unmet. This needs a ROADMAP SC3 amendment or an override decision to accept lazydocs Markdown-only as satisfying the documentation toolchain intent."
    artifacts:
      - path: "docs/"
        issue: "21 Markdown files exist via lazydocs; no HTML output; no docs/requirements_agent_tools/ subdirectory"
    missing:
      - "Either: update ROADMAP SC3 to remove the pdoc HTML requirement (accepted by user decision to use lazydocs); OR generate HTML output. Recommend: amend ROADMAP SC3 text to say 'docs/requirements_agent_tools.md exists (lazydocs Markdown API reference committed)' — matching the actual D-09 decision."
  - truth: ".pre-commit-config.yaml exists with hooks for ruff-format, ruff-check, ty, bandit, detect-private-key, interrogate (>=90%), pdoc, pytest"
    status: failed
    reason: ".pre-commit-config.yaml does not exist anywhere in the repository. Plans 00.5-03 and 00.5-04 (pre-commit pipeline and CLAUDE.md update) have not been planned or executed yet. This is a known planned gap — ROADMAP shows these as open plans."
    artifacts:
      - path: ".pre-commit-config.yaml"
        issue: "File does not exist"
    missing:
      - "Plan 00.5-03 must be planned and executed to create .pre-commit-config.yaml with 8 hooks"
  - truth: "CLAUDE.md references src/requirements_agent_tools/ (not shared/) and uses uv run <entry-point> invocations; Known Issues section cleaned of fixed items"
    status: failed
    reason: "CLAUDE.md has not been updated. It still: (1) references the old 'shared/' path in the Shared Library section, (2) contains a stale reference to 'skills/refine-requirements/scripts/refine.py' in the Skills section, (3) retains the Known Issues entry about 'skills/status-report/scripts/report.py.py double .py extension' which was fixed, (4) does not document the src/requirements_agent_tools/ package structure, and (5) does not contain uv run <entry-point> command examples. Plan 00.5-04 has not been planned or executed."
    artifacts:
      - path: "CLAUDE.md"
        issue: "Shared Library section references shared/ not src/requirements_agent_tools/; Skills section contains stale 'skills/refine-requirements/scripts/refine.py' reference; Known Issues has the fixed report.py.py entry; no entry-point command examples"
    missing:
      - "Plan 00.5-04 must be planned and executed to update CLAUDE.md with: src/requirements_agent_tools/ structure, uv run <entry-point> invocations, agent.yaml fallback pattern explanation, and removal of fixed Known Issues"
  - truth: "uv run interrogate -v --fail-under 90 src/requirements_agent_tools/ exits 0"
    status: failed
    reason: "Interrogate reports 50.6% docstring coverage against the 90% threshold. Command exits non-zero. This is a known planned gap — the pre-commit hook enforcing 90% is part of Plan 00.5-03, and SUMMARY-02 explicitly notes 'Doc coverage reflects current docstring coverage (~51%)'. Dev tools (lazydocs, interrogate) are declared in pyproject.toml and installed, but the coverage gate is not yet enforced or passing."
    artifacts:
      - path: "src/requirements_agent_tools/"
        issue: "50.6% docstring coverage; 82/166 public API items undocumented; fails at 90% threshold"
    missing:
      - "Docstring coverage must be raised to >= 90% before this criterion can pass. This work is expected in Plan 00.5-03 or before its pre-commit hook activates."
deferred:
  - truth: ".pre-commit-config.yaml exists with 8 hooks"
    addressed_in: "Phase 0.5 Plan 00.5-03"
    evidence: "ROADMAP: '00.5-03: Configure .pre-commit-config.yaml with 8 hooks (ruff, ty, bandit, interrogate, lazydocs, pytest, detect-private-key); add dev deps' — explicitly listed as a remaining open plan"
  - truth: "CLAUDE.md updated to reference src/requirements_agent_tools/"
    addressed_in: "Phase 0.5 Plan 00.5-04"
    evidence: "ROADMAP: '00.5-04: Update CLAUDE.md — new package structure, entry-point commands, agent fallback pattern, clean Known Issues' — explicitly listed as a remaining open plan"
human_verification: []
---

# Phase 0.5: Package Scaffold Verification Report

**Phase Goal:** Solidify `requirements_agent_tools` as the canonical CLI package: update all 6 skill command surfaces to use installed entry points, remove dead `scripts/` directories, add a documentation toolchain (pdoc + committed `docs/`), configure a comprehensive pre-commit pipeline, verify the agent.yaml fallback pattern, and update CLAUDE.md to reflect the new package structure.
**Verified:** 2026-04-28T14:30:00Z
**Status:** gaps_found
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| SC1 | `uv run init-project --help`, `uv run refine --help`, `uv run req-ops --help`, `uv run review --help`, `uv run report --help`, `uv run meeting --help` all exit 0 | VERIFIED | All 6 commands return usage output; confirmed by behavioral spot-check |
| SC2 | `find skills -name 'scripts' -type d` returns nothing | VERIFIED | Command returns empty; audit file 00.5-01-AUDIT.txt confirms; verified live |
| SC3 | `docs/requirements_agent_tools/index.html` exists AND `docs/requirements_agent_tools.md` exists | PARTIAL | `docs/requirements_agent_tools.md` exists (21 Markdown files via lazydocs). `docs/requirements_agent_tools/index.html` DOES NOT EXIST — pdoc HTML was never generated; D-09 decision switched to lazydocs (Markdown-only) but ROADMAP SC3 text was not updated |
| SC4 | `.pre-commit-config.yaml` exists with hooks for ruff-format, ruff-check, ty, bandit, detect-private-key, interrogate (>=90%), pdoc, pytest | FAILED | File does not exist. Plans 00.5-03 and 00.5-04 not yet planned or executed |
| SC5 | `CLAUDE.md` references `src/requirements_agent_tools/` (not `shared/`) and uses `uv run <entry-point>` invocations; Known Issues cleaned | FAILED | CLAUDE.md unchanged: still references `shared/`, still has stale `skills/refine-requirements/scripts/refine.py` reference, still has the fixed `report.py.py` Known Issue |
| SC6 | `uv run interrogate -v --fail-under 90 src/requirements_agent_tools/` exits 0 | FAILED | Exits 1; actual coverage is 50.6% (84/166 items documented) |

**Score:** 2.5/6 truths verified (SC1 VERIFIED, SC2 VERIFIED, SC3 PARTIAL, SC4 FAILED, SC5 FAILED, SC6 FAILED)

Note: The 3/6 score in the frontmatter counts SC3 as partial-pass since the core doc toolchain intent was met (Markdown docs committed). The ROADMAP SC3 text is an outdated contract that predates the D-09 lazydocs decision.

### Deferred Items

Items not yet met but explicitly addressed in later Phase 0.5 plans.

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | `.pre-commit-config.yaml` with 8 hooks | Phase 0.5 Plan 00.5-03 | ROADMAP: "00.5-03: Configure .pre-commit-config.yaml with 8 hooks (ruff, ty, bandit, interrogate, lazydocs, pytest, detect-private-key); add dev deps" |
| 2 | CLAUDE.md updated to reference `src/requirements_agent_tools/` | Phase 0.5 Plan 00.5-04 | ROADMAP: "00.5-04: Update CLAUDE.md — new package structure, entry-point commands, agent fallback pattern, clean Known Issues" |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `skills/new-project-initiation/SKILL.md` | init-project entry-point command surface | VERIFIED | Contains `uv run init-project new`, `list`, `update` (5 hits, >= 3 required) |
| `skills/refine-requirements/SKILL.md` | refine entry-point command surface | VERIFIED | Contains `uv run refine pending`, `show`, `apply`, `coverage` (5 hits, >= 4 required) |
| `skills/project-update/SKILL.md` | req-ops entry-point command surface | VERIFIED | Contains all 7 subcommands: add, update, get, list, search, history, vector |
| `skills/review-requirements/SKILL.md` | review entry-point command surface | VERIFIED | Contains `uv run review report`, `gaps`, `conflicts`, `coverage`; full frontmatter with `license: MIT` |
| `skills/status-report/SKILL.md` | report entry-point command surface | VERIFIED | Contains `uv run report generate`, `save`; full frontmatter with `license: MIT` |
| `skills/meeting-agent/SKILL.md` | meeting entry-point command surface | VERIFIED | Contains all 7 subcommands; `allowed-tools` added as deviation |
| `pyproject.toml` | dev-dep declaration for lazydocs + 4 others | VERIFIED | `[dependency-groups] dev` with lazydocs>=0.4.8, pre-commit>=4.6.0, ty>=0.0.32, bandit>=1.9.4, interrogate>=1.7.0 |
| `docs/requirements_agent_tools.md` | Top-level package overview | VERIFIED | Exists; generated by lazydocs |
| `docs/` | Generated Markdown API reference (>= 10 files) | VERIFIED | 21 Markdown files; includes 9 db/ submodule docs; no HTML files |
| `.pre-commit-config.yaml` | Pre-commit pipeline configuration | MISSING | File does not exist; Plan 00.5-03 not executed |
| `CLAUDE.md` | Updated with src/ structure, entry-point commands | STUB | File exists but not updated; still references old `shared/` paths |
| `.planning/phases/00.5-package-scaffold/00.5-01-AUDIT.txt` | Skill directory audit evidence | VERIFIED | Exists with all required sections |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `skills/*/SKILL.md` | `pyproject.toml [project.scripts]` | `uv run <entry-point>` matches a key in [project.scripts] | WIRED | All 6 SKILL.md files use entry points (init-project, refine, req-ops, review, report, meeting) that are defined in pyproject.toml [project.scripts] |
| `pyproject.toml [dependency-groups] dev` | `lazydocs CLI` | `uv run --with lazydocs lazydocs` | WIRED | lazydocs>=0.4.8 declared; `uv run --with lazydocs lazydocs --version` works |
| `src/requirements_agent_tools/` | `docs/` | lazydocs walks the package and writes one Markdown file per module | WIRED | 21 Markdown files generated; 9 cover db/ subpackage (confirmed by `grep -l 'requirements_agent_tools.db' docs/*.md`) |
| `.pre-commit-config.yaml` | pre-commit hooks | pre-commit runs on commit | NOT_WIRED | File does not exist |

### Data-Flow Trace (Level 4)

Not applicable for this phase — phase produces configuration files and documentation, not components that render dynamic data.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| init-project exits 0 | `uv run init-project --help` | `usage: init-project [-h] {new,list,update}` | PASS |
| refine exits 0 | `uv run refine --help` | `usage: refine [-h] [--project PROJECT] {pending,show,apply,coverage}` | PASS |
| req-ops exits 0 | `uv run req-ops --help` | `usage: req-ops [-h] [--project PROJECT] {add,update,get,list,search,history,vector}` | PASS |
| review exits 0 | `uv run review --help` | Usage with subcommands | PASS |
| report exits 0 | `uv run report --help` | Usage with subcommands | PASS |
| meeting exits 0 | `uv run meeting --help` | Usage with subcommands | PASS |
| No scripts/ dirs | `find skills -name scripts -type d` | Empty output | PASS |
| No stale paths | `grep -rE '(python\|uv run) skills/.*/scripts/' skills/` | No matches | PASS |
| interrogate at 90% | `uv run interrogate -v --fail-under 90 src/requirements_agent_tools/` | 50.6% actual; exits 1 | FAIL |
| docs/ Markdown count | `find docs -maxdepth 2 -name '*.md' \| wc -l` | 21 | PASS |
| no HTML in docs/ | `find docs -name '*.html'` | Empty | PASS |

### Requirements Coverage

| Requirement | Source Plan | Status | Evidence |
|-------------|------------|--------|----------|
| PKG-01: Entry-point migration | 00.5-01 | SATISFIED | All 6 SKILL.md files use `uv run <entry-point>`; no stale paths remain |
| PKG-02: scripts/ removal | 00.5-01 | SATISFIED | `find skills -name scripts -type d` returns empty; audit committed |
| PKG-03: Documentation toolchain | 00.5-02 | SATISFIED (with SC3 caveat) | lazydocs declared as dev dep; 21 Markdown docs committed; no HTML per D-10 decision; ROADMAP SC3 text predates D-09 decision switch |
| PKG-04: Pre-commit pipeline | 00.5-03 (not yet planned) | BLOCKED | `.pre-commit-config.yaml` does not exist |
| PKG-05: agent.yaml fallback verification | 00.5-04 (not yet planned) | NEEDS HUMAN | `agent.yaml` fallback list exists (`openai/gpt-5.2`, `openai/gpt-oss-20b`, `google/gemma-4-31b`); CLAUDE.md does not yet document the gitagent fallback pattern per D-08; verification of whether the fallback list is "sensible" requires human judgment |
| PKG-06: CLAUDE.md update | 00.5-04 (not yet planned) | BLOCKED | CLAUDE.md still references `shared/`, stale `scripts/refine.py` path, and the fixed Known Issue |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `CLAUDE.md` | 41 | `skills/refine-requirements/scripts/refine.py` — stale path to deleted file | Warning | Agent reads CLAUDE.md; this stale reference will mislead the agent about the CLI surface |
| `CLAUDE.md` | 48-52 | Entire `### Shared Library (shared/)` section references old `shared/` path; package now lives at `src/requirements_agent_tools/` | Warning | Architecture description is incorrect; misleads future developers and the agent |
| `CLAUDE.md` | 73 | Known Issue: `skills/status-report/scripts/report.py.py double .py extension` — issue is fixed (no scripts/ dir exists) | Info | Stale Known Issue adds noise |
| `skills/elicit-requirements/SKILL.md` | — | File is root-owned (flagged in audit); out-of-scope for Plans 01/02 | Info | Not blocking current phase; deferred per audit note |

### Human Verification Required

None — all gaps are deterministically verifiable from the codebase. The PKG-05 agent.yaml fallback "sensibility" question is noted but the structural presence of the fallback list is confirmed.

---

## Gaps Summary

**Root cause:** Phase 0.5 has 4 plans. Plans 01 and 02 were planned and executed successfully (PKG-01, PKG-02, PKG-03 met). Plans 03 and 04 (pre-commit pipeline and CLAUDE.md update) have not been planned yet. Three of the six ROADMAP success criteria require these unexecuted plans.

**SC3 Special Case:** The ROADMAP SC3 text predates the in-flight decision to switch from pdoc to lazydocs (CONTEXT.md D-09, revised 2026-04-28). The SC3 contract requires `docs/requirements_agent_tools/index.html` (pdoc HTML) which was deliberately never generated. The actual deliverable — 21 Markdown files via lazydocs — satisfies the documentation toolchain intent but not the literal SC3 wording. This is a ROADMAP contract inconsistency, not a delivery failure. The next planner should either update ROADMAP SC3 text or add a verification override.

**Grouped gaps by work remaining:**

1. **Plan 00.5-03 work** — `.pre-commit-config.yaml` (SC4) and docstring coverage to 90% (SC6). These are coupled: the interrogate hook in pre-commit enforces the 90% gate, but the coverage itself must be written before the hook can pass.

2. **Plan 00.5-04 work** — CLAUDE.md update (SC5): remove `shared/` references, remove stale `scripts/refine.py` mention, remove fixed Known Issue, add `src/requirements_agent_tools/` architecture section, add entry-point commands, add agent.yaml fallback pattern explanation.

3. **ROADMAP SC3 amendment** — SC3 text still says `pdoc HTML + Markdown`. Recommend amending to: `docs/requirements_agent_tools.md exists (lazydocs Markdown API reference committed, 21 files)`.

---

_Verified: 2026-04-28T14:30:00Z_
_Verifier: Claude (gsd-verifier)_
