---
phase: 0.5
slug: package-scaffold
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-28
---

# Phase 0.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 9.0.3 (already installed) |
| **Config file** | `pyproject.toml [tool.pytest.ini_options]` |
| **Quick run command** | `uv run pytest --tb=short -q` |
| **Full suite command** | `uv run pytest` |
| **Estimated runtime** | ~5 seconds |

---

## Sampling Rate

- **After every task commit:** Run `uv run pytest --tb=short -q`
- **After every plan wave:** Run `uv run pytest`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 10 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 00.5-01-01 | 01 | 1 | PKG-01 | — | No `python skills/*/scripts/` references | manual | `grep -r "python skills" skills/` returns empty | ✅ | ⬜ pending |
| 00.5-01-02 | 01 | 1 | PKG-02 | — | No scripts/ dirs exist | manual | `find skills -name scripts -type d` returns empty | ✅ | ⬜ pending |
| 00.5-02-01 | 02 | 1 | PKG-03 | — | docs/ generated from package | manual | `ls docs/requirements_agent_tools*.md` succeeds | ❌ W0 | ⬜ pending |
| 00.5-03-01 | 03 | 1 | PKG-04 | — | pre-commit hooks pass on clean commit | manual | `uv run pre-commit run --all-files` exits 0 | ❌ W0 | ⬜ pending |
| 00.5-03-02 | 03 | 1 | PKG-06 | — | interrogate 90% threshold passes | unit | `uv run interrogate -v --fail-under 90 src/requirements_agent_tools/` exits 0 | ❌ W0 | ⬜ pending |
| 00.5-04-01 | 04 | 2 | PKG-05 | — | CLAUDE.md references src/ layout | manual | `grep "src/requirements_agent_tools" CLAUDE.md` matches | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements. This phase does not add new logic modules — it updates documentation, SKILL.md files, and toolchain configuration. No new test files are required. The pre-commit pytest hook validates that existing 104 tests continue passing.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| All 6 SKILL.md files use `uv run <entry-point>` | PKG-01 | Text content check | `grep -h "uv run" skills/*/SKILL.md` shows all 6 entry points |
| `scripts/` directories removed | PKG-02 | Filesystem check | `find skills -name scripts -type d` returns nothing |
| docs/ committed to repo | PKG-03 | Git check | `git ls-files docs/` lists Markdown files |
| CLAUDE.md cleaned of stale refs | PKG-05 | Content review | No `shared/` in Commands section; no double-ext Known Issues |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 10s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
