---
phase: 0.5
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - pyproject.toml
  - docs/
autonomous: true
requirements:
  - PKG-03
must_haves:
  truths:
    - "lazydocs is declared as a dev dependency in pyproject.toml [dependency-groups] dev"
    - "docs/ directory exists at repo root and is committed to git"
    - "docs/ contains Markdown files generated from src/requirements_agent_tools (full package coverage including db/ subpackage)"
    - "Running `uv run --with lazydocs lazydocs --output-path docs/ requirements_agent_tools` regenerates docs/ deterministically (idempotent on a clean tree)"
  artifacts:
    - path: pyproject.toml
      provides: "dev-dep declaration for lazydocs"
      contains: "lazydocs"
    - path: docs/
      provides: "Generated Markdown API reference"
    - path: docs/requirements_agent_tools.md
      provides: "Top-level package overview (lazydocs default output)"
  key_links:
    - from: pyproject.toml [dependency-groups] dev
      to: lazydocs CLI
      via: "uv run --with lazydocs lazydocs"
      pattern: "uv run --with lazydocs"
    - from: src/requirements_agent_tools/
      to: docs/
      via: "lazydocs walks the package and writes one Markdown file per module"
      pattern: "docs/.*\\.md$"
---

<objective>
Add `lazydocs` as a dev dependency (D-09, revised 2026-04-28 from pdoc), generate Markdown documentation for the full `requirements_agent_tools` package (D-10/D-11/D-12), and commit the `docs/` output. Use Google-style docstring parsing (D-13).

Purpose: PKG-03. The repo must ship a committed `docs/` Markdown tree so reviewers can browse the API reference without running tooling. lazydocs was selected over pdoc because pdoc cannot emit Markdown (RESEARCH critical flag on D-10; resolved by user in CONTEXT.md D-09).

Output:
- pyproject.toml with `[dependency-groups] dev` containing `lazydocs>=0.4.8` (and pre-positioning the other dev tools that Plan 03 will need — see action)
- docs/ directory populated with Markdown files for every public module under `src/requirements_agent_tools/`, including the `db/` subpackage
- All files staged and ready to commit
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00.5-package-scaffold/00-5-CONTEXT.md
@.planning/phases/00.5-package-scaffold/00.5-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- lazydocs CLI surface — verified in PATTERNS.md and CONTEXT.md D-09. -->
<!-- lazydocs reads Google-style docstrings, writes one .md per module under --output-path. -->

Invocation:
  uv run --with lazydocs lazydocs --output-path docs/ requirements_agent_tools

Key flags:
  --output-path <DIR>          : destination directory (default: docs/)
  --src-base-url <URL>          : optional, prepended to "Source code" links
  <package>                     : positional, importable module name (NOT a filesystem path)

Why `uv run --with lazydocs` (not uvx): lazydocs imports the package at doc-gen
time. uvx runs in an isolated env with no project deps -> ImportError on loguru/pydantic.
This is the same constraint that applied to pdoc per RESEARCH Pattern 1 / Pitfall 1.

pyproject.toml — current shape (PATTERNS.md lines 286-323):
  [project] / [project.scripts] / [build-system] / [tool.hatch.build.targets.wheel] / [tool.pytest.ini_options]

Append blocks (PATTERNS.md lines 325-366):
  [dependency-groups]
  dev = [ "lazydocs>=0.4.8", "pre-commit>=4.6.0", "ty>=0.0.32", "bandit>=1.9.4", "interrogate>=1.7.0" ]
  [tool.interrogate] ...
  [tool.ty.environment] / [tool.ty.src] / [tool.ty.rules] ...

OWNERSHIP: pyproject.toml is root:root (verified by `ls -la pyproject.toml` 2026-04-28).
`uv add --dev` will fail with permission denied — chown first.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix pyproject.toml ownership and append [dependency-groups] dev + tool config sections</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml
    - .planning/phases/00.5-package-scaffold/00.5-PATTERNS.md  (section "pyproject.toml")
    - .planning/phases/00.5-package-scaffold/00-5-CONTEXT.md  (D-09 lazydocs revision; D-14..D-20 dev tools)
  </read_first>
  <action>
    Per CONTEXT D-09 and PATTERNS pyproject.toml block, add the dev dependency group and tool configuration sections. This task pre-positions the dev deps that Plan 03 will use; lazydocs is the only one needed in this plan but adding all 5 in one shot avoids re-editing pyproject.toml twice.

    Step 1 — fix ownership (PATTERNS ownership note + RESEARCH availability table):
      Run: `sudo chown $USER:$USER pyproject.toml`
      Verify: `ls -la pyproject.toml` shows `$USER $USER` ownership.

    Step 2 — append the following blocks to the END of pyproject.toml, AFTER the existing `[tool.pytest.ini_options]` block. Use the Edit tool — do NOT rewrite the whole file. Insert verbatim:

    ```toml

    [dependency-groups]
    dev = [
        "lazydocs>=0.4.8",
        "pre-commit>=4.6.0",
        "ty>=0.0.32",
        "bandit>=1.9.4",
        "interrogate>=1.7.0",
    ]

    [tool.interrogate]
    fail-under = 90
    ignore-init-method = true
    ignore-init-module = true
    ignore-magic = true
    ignore-private = true
    ignore-semiprivate = false
    verbose = 0
    quiet = false
    color = true
    exclude = ["tests", "docs"]

    [tool.ty.environment]
    python-version = "3.13"

    [tool.ty.src]
    include = ["src/requirements_agent_tools"]

    [tool.ty.rules]
    # ty flags sqlite3.Row dict-style access as invalid-argument-type
    # because the inferred type is Any | tuple[Any, ...].
    # Suppress as warn until sqlite-vec ships type stubs.
    invalid-argument-type = "warn"
    ```

    Step 3 — sync the lockfile and venv:
      Run: `uv sync`
      Expect exit 0. The dev group should now be installed (uv installs `--dev` deps by default with `uv sync`).

    Notes:
      - Do NOT run `uv add --dev lazydocs` — appending to pyproject.toml + `uv sync` is equivalent and avoids a second `uv add` round-trip.
      - Existing keys (project / project.scripts / build-system / tool.hatch / tool.pytest) are NOT modified.
      - If `uv sync` fails because lazydocs has not yet been published at >=0.4.8, drop the `>=0.4.8` constraint to just `"lazydocs"` and retry. The latest is whatever PyPI ships at execute time.
  </action>
  <verify>
    <automated>test -O pyproject.toml && grep -E '^\[dependency-groups\]' pyproject.toml && grep -E '^\s*"lazydocs' pyproject.toml && grep -E '^\[tool\.interrogate\]' pyproject.toml && grep -E '^\[tool\.ty\.environment\]' pyproject.toml && grep -E '^\[tool\.ty\.src\]' pyproject.toml && grep -E '^\[tool\.ty\.rules\]' pyproject.toml && uv run --with lazydocs lazydocs --version</automated>
  </verify>
  <acceptance_criteria>
    - `ls -la pyproject.toml` shows ownership `$USER $USER` (not root).
    - `grep -E '^\[dependency-groups\]' pyproject.toml` matches.
    - `grep '"lazydocs' pyproject.toml` matches (with or without version constraint).
    - All 5 dev tools listed: `grep -E '"(lazydocs|pre-commit|ty|bandit|interrogate)' pyproject.toml | wc -l` returns 5.
    - `grep -E '^\[tool\.(interrogate|ty\.environment|ty\.src|ty\.rules)\]' pyproject.toml | wc -l` returns 4.
    - `uv sync` exits 0.
    - `uv run --with lazydocs lazydocs --version` exits 0 (proof lazydocs is invokable in the project venv).
    - The `[project]`, `[project.scripts]`, `[build-system]`, `[tool.hatch.build.targets.wheel]`, and `[tool.pytest.ini_options]` blocks are unchanged: `git diff pyproject.toml` shows ONLY appended lines after `[tool.pytest.ini_options]`.
  </acceptance_criteria>
  <done>pyproject.toml owned by $USER, contains all 5 dev deps + interrogate/ty config; `uv sync` succeeds; `lazydocs --version` runs in the project venv.</done>
</task>

<task type="auto">
  <name>Task 2: Generate docs/ Markdown tree from src/requirements_agent_tools using lazydocs</name>
  <files>docs/</files>
  <read_first>
    - pyproject.toml  (must contain dependency-groups dev with lazydocs from Task 1)
    - .planning/phases/00.5-package-scaffold/00-5-CONTEXT.md  (D-10 Markdown only; D-11 docs/ destination; D-12 full package; D-13 Google docstrings)
    - .planning/phases/00.5-package-scaffold/00.5-PATTERNS.md  (.pre-commit-config.yaml block — same lazydocs invocation)
  </read_first>
  <action>
    Per D-09/D-10/D-11/D-12, generate a Markdown documentation tree for the full `requirements_agent_tools` package.

    Step 1 — clean any pre-existing docs/ directory (was never committed; safe to wipe):
      ```
      rm -rf docs/
      mkdir docs/
      ```

    Step 2 — invoke lazydocs from the repo root:
      ```
      uv run --with lazydocs lazydocs \
        --output-path docs/ \
        requirements_agent_tools
      ```

      Note: `requirements_agent_tools` is the importable PACKAGE name (not a filesystem path). lazydocs imports the package and walks its `__all__` / public names.

      The default lazydocs output is GitHub-flavored Markdown. Each module produces one `.md` file (e.g., `requirements_agent_tools.models.md`). Subpackages (`db/`) produce one file per submodule (e.g., `requirements_agent_tools.db.requirements.md`).

    Step 3 — verify the tree:
      ```
      ls docs/
      ```
      Expect at minimum:
        - requirements_agent_tools.md   (top-level package summary)
        - requirements_agent_tools.<module>.md for each module: CONSTANTS, _cli_io, init_project, meeting, models, project_md, project_md_cli, project_session, refine, report, req_ops, review
        - requirements_agent_tools.db.<submodule>.md for each db submodule: connection, schema, projects, requirements, minutes, updates, embeddings, _serialization, _logging, cli

    Step 4 — handle the lazydocs `index.md` file (if generated):
      lazydocs may or may not produce an `index.md` depending on flags. Leave whatever it generates — do not hand-edit `docs/`.

    Step 5 — sanity check Google docstring parsing:
      `grep -l 'Args:' docs/requirements_agent_tools.models.md` — if no `Args:` blocks render, the docstrings are not yet Google-style. lazydocs handles Google docstrings natively (per CONTEXT D-13 and lazydocs README); if rendering looks broken, the issue is missing/incomplete docstrings, NOT lazydocs config. Pre-commit interrogate (Plan 03) and the CLAUDE.md docstring guidance (Plan 04) will drive coverage up over time.

    Step 6 — stage docs/ for commit (the wrap-up commit at the end of the plan handles the actual `git commit`):
      ```
      git add docs/
      ```
      (Do NOT commit yet — keep all wave-2 changes consolidated for the orchestrator.)

    Failure mode: if lazydocs fails with `ModuleNotFoundError: No module named '<dep>'`, the package was not installed in the active venv. Run `uv sync` and retry. If it fails on a specific module import (e.g., `db.embeddings` due to sqlite_vec), retry with `--validate=False` flag (lazydocs supports skipping import validation per its README); if that flag is rejected, the module's import path itself is broken — flag it for remediation before continuing.
  </action>
  <verify>
    <automated>test -d docs/ && test -f docs/requirements_agent_tools.md && [ "$(find docs -maxdepth 2 -name '*.md' | wc -l)" -ge 10 ] && grep -l 'requirements_agent_tools.db' docs/*.md 2>/dev/null | head -1 | grep -q .</automated>
  </verify>
  <acceptance_criteria>
    - `docs/` directory exists at repo root.
    - `docs/requirements_agent_tools.md` exists.
    - `find docs -maxdepth 2 -name '*.md' | wc -l` is >= 10 (one per top-level module + several db submodules).
    - At least one docs file contains a reference to a `db` submodule (proof D-12 full-package scope including db/ was honored): `grep -l 'requirements_agent_tools.db' docs/*.md` returns at least one match.
    - No HTML files in docs/ (D-10 Markdown only): `find docs -name '*.html'` returns empty.
    - `git status docs/` shows files staged (after `git add docs/`).
  </acceptance_criteria>
  <done>docs/ contains the full Markdown API reference for requirements_agent_tools (including db/ subpackage); files are staged for commit; no HTML files present.</done>
</task>

</tasks>

<verification>
PKG-03: `docs/requirements_agent_tools.md` exists and a full Markdown tree covers all public modules. Verified by:
```
test -f docs/requirements_agent_tools.md
find docs -maxdepth 2 -name '*.md' | wc -l   # >= 10
find docs -name '*.html'                      # empty (D-10)
```

D-09 supersession: pyproject.toml lists `lazydocs` (not `pdoc`) as a dev dep. Verified by:
```
grep '"lazydocs' pyproject.toml
! grep '"pdoc' pyproject.toml
```

D-12 full package scope: at least one db submodule doc exists. Verified by:
```
grep -l 'requirements_agent_tools.db' docs/*.md | head -1 | grep -q .
```
</verification>

<success_criteria>
- pyproject.toml owned by $USER, contains [dependency-groups] dev with lazydocs + 4 other dev tools.
- pyproject.toml contains [tool.interrogate] and [tool.ty.*] sections (pre-positioned for Plan 03).
- `uv run --with lazydocs lazydocs --version` works in the project venv.
- docs/ contains Markdown files generated from the full requirements_agent_tools package.
- docs/ is staged for commit.
- No `pdoc` references introduced anywhere (D-09 supersession).
</success_criteria>

<output>
After completion, create `.planning/phases/00.5-package-scaffold/0.5-02-SUMMARY.md` documenting:
- Ownership change to pyproject.toml ($USER:$USER).
- Dev deps added (lazydocs + pre-commit + ty + bandit + interrogate).
- Tool config sections added ([tool.interrogate], [tool.ty.environment], [tool.ty.src], [tool.ty.rules]).
- lazydocs invocation command and number of Markdown files generated.
- Confirmation that no HTML output was produced (D-10 Markdown-only).
- Confirmation that PKG-03 success criteria are met.
</output>
