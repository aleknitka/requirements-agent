---
phase: 01-project-initialisation
plan: 05
type: execute
wave: 3
depends_on:
  - 01-03
  - 01-04
files_modified:
  - src/requirements_agent_tools/refine.py
  - src/requirements_agent_tools/req_ops.py
  - src/requirements_agent_tools/review.py
  - src/requirements_agent_tools/report.py
  - src/requirements_agent_tools/meeting.py
  - src/requirements_agent_tools/db/cli.py
  - src/requirements_agent_tools/project_md_cli.py
autonomous: true
requirements:
  - INIT-05
  - INIT-06

must_haves:
  truths:
    - "All 7 CLI modules have no --project flag"
    - "All 7 CLI modules call get_project_conn() instead of ps.resolve(args.project)"
    - "db/cli.py root group has no --project click option"
    - "db/cli.py _open_conn() takes no arguments and uses C.DB_PATH"
    - "project_md_cli.py uses get_project_conn() and calls project_md.save/read/append without slug arg"
    - "uv run refine --help exits 0"
    - "uv run req-ops --help exits 0"
    - "uv run review --help exits 0"
    - "uv run report --help exits 0"
    - "uv run meeting --help exits 0"
  artifacts:
    - path: "src/requirements_agent_tools/refine.py"
      provides: "slug-free refine CLI"
    - path: "src/requirements_agent_tools/req_ops.py"
      provides: "slug-free req-ops CLI"
    - path: "src/requirements_agent_tools/review.py"
      provides: "slug-free review CLI"
    - path: "src/requirements_agent_tools/report.py"
      provides: "slug-free report CLI"
    - path: "src/requirements_agent_tools/meeting.py"
      provides: "slug-free meeting CLI"
    - path: "src/requirements_agent_tools/db/cli.py"
      provides: "slug-free db CLI"
    - path: "src/requirements_agent_tools/project_md_cli.py"
      provides: "slug-free project_md CLI"
  key_links:
    - from: "all 7 CLI modules"
      to: "project_session.get_project_conn()"
      via: "from .project_session import get_project_conn; conn = get_project_conn()"
      pattern: "get_project_conn"
    - from: "db/cli.py _open_conn()"
      to: "CONSTANTS.DB_PATH"
      via: "get_db(str(C.DB_PATH))"
      pattern: "DB_PATH"
---

<objective>
Remove the `--project <slug>` flag from all 7 CLI modules and wire `get_project_conn()` everywhere. This is the final migration step — after this plan all CLI code is slug-free.

Purpose: Phase 1 success criterion INIT-06 requires all CLIs to work without `--project`. Plan 04 created `get_project_conn()`; this plan wires it to every consumer.
Output: 7 updated CLI modules. All `--help` commands work; no `--project` flag visible.
</objective>

<execution_context>
@/home/aleksander/.claude/get-shit-done/workflows/execute-plan.md
@/home/aleksander/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-project-initialisation/01-CONTEXT.md
@.planning/phases/01-project-initialisation/01-PATTERNS.md
@.planning/phases/01-project-initialisation/01-03-SUMMARY.md
@.planning/phases/01-project-initialisation/01-04-SUMMARY.md
</context>

<interfaces>
<!-- Contracts from prior plans that this plan depends on. -->

From project_session.py (Plan 04 output):
```python
def get_project_conn() -> sqlite3.Connection:
    """Open the single project DB. Exits with error if not initialised."""
```

From project_md.py (Plan 03 output):
```python
def save(conn, content, *, changed_by, summary) -> Path: ...       # no slug arg
def append_section(conn, section, *, ...) -> Path: ...              # no slug arg
def read() -> Optional[str]: ...                                     # no slug arg
```

From CONSTANTS.py (Plan 02 output):
```python
DB_PATH: Path   # PROJECT_DIR / "project.db"
MD_PATH: Path   # PROJECT_DIR / "PROJECT.md"
```

From db/projects.py (Plan 03 output):
```python
def get_project(conn) -> Optional[ProjectMeta]: ...   # still exists
# REMOVED: get_project_by_slug, discover_projects
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Remove --project from refine.py, req_ops.py, review.py, meeting.py</name>
  <files>
    src/requirements_agent_tools/refine.py,
    src/requirements_agent_tools/req_ops.py,
    src/requirements_agent_tools/review.py,
    src/requirements_agent_tools/meeting.py
  </files>
  <read_first>
    src/requirements_agent_tools/refine.py,
    src/requirements_agent_tools/req_ops.py,
    src/requirements_agent_tools/review.py,
    src/requirements_agent_tools/meeting.py,
    src/requirements_agent_tools/project_session.py
  </read_first>
  <action>
Apply the same change to all four files. The change is mechanical and identical in each.

**Universal pattern for each file:**

A. Replace the import:
```python
# BEFORE:
from . import project_session as ps
# AFTER:
from .project_session import get_project_conn
```

B. Replace every `ps.resolve(args.project)` call:
```python
# BEFORE:
_, conn, _ = ps.resolve(args.project)
# or:
slug, conn, meta = ps.resolve(args.project)
# AFTER:
conn = get_project_conn()
```

C. Remove `p.add_argument("--project", default=None)` from `build_parser()`.

D. Handle any remaining uses of the unpacked `slug` or `meta` variables:
- If `slug` was only used to pass to `ps.resolve` or put in output dicts: remove it.
- If `meta` is used in the function body (e.g., `review.py` passes it to `_check_gaps(reqs, meta)`): fetch it separately using `from .db.projects import get_project as _get_project` and `meta = _get_project(conn)`. If `meta` may be None (projects table empty after `init-project setup`), use a fallback: `meta = _get_project(conn)`.

**File-specific notes:**

`refine.py`:
- 4 call sites: `cmd_pending`, `cmd_show`, `cmd_apply`, `cmd_coverage`
- All use pattern `_, conn, _ = ps.resolve(args.project)` — simple replacement, no meta/slug used in bodies

`req_ops.py`:
- 7 call sites: `cmd_add`, `cmd_update`, `cmd_get`, `cmd_list`, `cmd_search`, `cmd_history`, `cmd_vector`
- `slug` appears in `_ok()` output dicts (e.g., `"slug": slug`) — remove these dict keys
- `meta` appears in some `_ok()` output dicts (e.g., `"project": meta.name`) — remove or replace with empty string
- Also imports `from . import CONSTANTS as C` — check if `C` is still used after removing slug-derived paths; if not, remove the import

`review.py`:
- 4 call sites: `cmd_gaps`, `cmd_conflicts`, `cmd_coverage`, `cmd_report`
- `meta` is passed to `_check_gaps(reqs, meta)`, `_check_coverage(reqs, meta)`, and used in `_ok()` output
- Add import: `from .db.projects import get_project as _get_project`
- Fetch meta per command: `meta = _get_project(conn)` after `conn = get_project_conn()`
- Keep type annotation on `_check_gaps(reqs, meta)` etc. — `meta` type is `Optional[ProjectMeta]`, handle None case

`meeting.py`:
- 7 call sites: `cmd_log`, `cmd_get`, `cmd_list`, `cmd_decisions`, `cmd_update_decision`, `cmd_close_action`, `cmd_integrate`
- All use pattern `_, conn, _ = ps.resolve(args.project)` — simple replacement
- `upsert_project` is imported for `cmd_integrate` (writes status summary back) — this is unrelated to slug; keep it

Maintain Google-style docstrings on all modified `cmd_*` functions — remove any mention of `--project` flag from docstrings.
  </action>
  <verify>
    <automated>cd /home/aleksander/github/requirements-agent && python -c "
import importlib, inspect
for mod_name in ['refine', 'req_ops', 'review', 'meeting']:
    m = importlib.import_module(f'requirements_agent_tools.{mod_name}')
    src = inspect.getsource(m)
    assert 'args.project' not in src, f'{mod_name}: args.project still present'
    assert 'ps.resolve' not in src, f'{mod_name}: ps.resolve still present'
    assert 'get_project_conn' in src, f'{mod_name}: get_project_conn not imported'
    print(f'PASS: {mod_name}')
"</automated>
  </verify>
  <done>
    - For each of the 4 files: `grep -c "args\.project\|ps\.resolve" src/requirements_agent_tools/{refine,req_ops,review,meeting}.py` returns 0
    - `grep -c "get_project_conn" src/requirements_agent_tools/refine.py` returns >= 1
    - `uv run refine --help` exits 0 and does NOT show `--project`
    - `uv run req-ops --help` exits 0 and does NOT show `--project`
    - `uv run review --help` exits 0 and does NOT show `--project`
    - `uv run meeting --help` exits 0 and does NOT show `--project`
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove --project from report.py, db/cli.py, project_md_cli.py</name>
  <files>
    src/requirements_agent_tools/report.py,
    src/requirements_agent_tools/db/cli.py,
    src/requirements_agent_tools/project_md_cli.py
  </files>
  <read_first>
    src/requirements_agent_tools/report.py,
    src/requirements_agent_tools/db/cli.py,
    src/requirements_agent_tools/project_md_cli.py,
    src/requirements_agent_tools/CONSTANTS.py,
    src/requirements_agent_tools/project_md.py
  </read_first>
  <action>
**report.py:**

1. `_build_report(slug, conn, meta)` signature — remove `slug` parameter:
   ```python
   def _build_report(conn, meta) -> dict:
   ```
   Inside `_build_report()`, any use of `slug` for path construction (e.g., file references) must use `C.PROJECT_DIR` instead. Read the full function body to identify any `slug` usages.

2. In `cmd_generate()` and `cmd_save()`: replace `slug, conn, meta = ps.resolve(args.project)` with:
   ```python
   conn = get_project_conn()
   meta = _get_project(conn)
   ```
   Add `from .db.projects import get_project as _get_project` to imports.

3. Update the `_build_report()` call sites to remove `slug` argument: `_build_report(conn, meta)`.

4. Remove `p.add_argument("--project", default=None)` from `build_parser()`.

5. Replace `from . import project_session as ps` with `from .project_session import get_project_conn`.

6. Check if `from . import CONSTANTS as C` is still needed after removing slug-derived path calls. If `C.PROJECT_DIR` is used in `cmd_save()` for writing report files, keep it; otherwise remove.

---

**db/cli.py:**

This is a click CLI. The `--project` option is declared on the root group. Read the full file before making changes.

1. Remove the `@click.option("--project", ...)` decorator from the `cli` group entirely.

2. Remove `slug: str` parameter from `cli()` function signature.

3. Remove `ctx.obj["slug"] = slug` from `cli()` body.

4. Update `_open_conn(slug: str)` to `_open_conn()` — remove slug parameter. Change the body:
   ```python
   def _open_conn() -> sqlite3.Connection:
       """Open the single project database."""
       return get_db(str(C.DB_PATH))
   ```

5. Update all call sites of `_open_conn(ctx.obj["slug"])` to `_open_conn()`. These appear inside subcommand functions — search for `_open_conn(` in the file and remove the argument.

6. Update all references to `ctx.obj["slug"]` in subcommand bodies — if used for output dicts, remove or replace with a static identifier.

7. Update the module-level docstring examples: `db --project demo project show` becomes `db project show`.

8. If `@click.pass_context` on `cli()` is only used to store/retrieve `ctx.obj["slug"]`, and no other subcommand needs `ctx`, remove `@click.pass_context` from `cli()`. If any subcommand still uses `@click.pass_context` for other purposes, keep it. Read the full file to determine this.

9. Add `from ..project_session import get_project_conn` import if needed, or rely on `_open_conn()` using `C.DB_PATH` directly via `get_db`. The `_open_conn()` pattern using `C.DB_PATH` directly is sufficient — no need to import `get_project_conn` in db/cli.py since `_open_conn()` can use `get_db(str(C.DB_PATH))` directly.

---

**project_md_cli.py:**

1. Replace `from . import project_session as ps` with `from .project_session import get_project_conn`.

2. In `cmd_save()`: replace `slug, conn, _ = ps.resolve(args.project)` with `conn = get_project_conn()`. Update the `project_md.save()` call to remove `slug`: `project_md.save(conn, content, changed_by=args.by, summary=args.summary)`.

3. Update the `_ok()` output: replace `"slug": slug` with `"path": str(C.MD_PATH)` (import `from . import CONSTANTS as C` if not already imported).

4. In `cmd_append()`: replace `slug, conn, _ = ps.resolve(args.project)` with `conn = get_project_conn()`. Update `project_md.append_section(conn, slug, section, ...)` to `project_md.append_section(conn, section, ...)`.

5. In `cmd_read()`: replace `slug, _, _ = ps.resolve(args.project)` with `conn = get_project_conn()` (even though conn isn't used in read, the existence check is needed). Update `project_md.read(slug)` to `project_md.read()`.

6. Remove `p.add_argument("--project", default=None)` from `build_parser()`.

7. Update output dicts that contained `"slug": slug` to use `"path": str(C.MD_PATH)` instead.
  </action>
  <verify>
    <automated>cd /home/aleksander/github/requirements-agent && python -c "
import importlib, inspect
for mod_name in ['report']:
    m = importlib.import_module(f'requirements_agent_tools.{mod_name}')
    src = inspect.getsource(m)
    assert 'args.project' not in src, f'{mod_name}: args.project still present'
    assert 'ps.resolve' not in src, f'{mod_name}: ps.resolve still present'
    print(f'PASS: {mod_name}')
from requirements_agent_tools.db import cli as db_cli
src = inspect.getsource(db_cli)
assert '--project' not in src, 'db/cli.py: --project still present'
assert 'db_path' not in src, 'db/cli.py: db_path still present'
print('PASS: db/cli.py')
from requirements_agent_tools import project_md_cli as pmc
src = inspect.getsource(pmc)
assert 'ps.resolve' not in src, 'project_md_cli: ps.resolve still present'
print('PASS: project_md_cli.py')
"